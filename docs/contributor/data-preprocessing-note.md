# Data Preprocessing Contributor Note

This branch keeps a small contributor workflow note for the
`feature/data-preprocessing` workstream.

The note is documentation-only and records that branch updates should remain focused
on preprocessing review context before they are proposed for integration.
